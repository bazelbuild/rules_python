load("//python:versions.bzl", "PLATFORMS", "TOOL_VERSIONS")
load(":platforms.bzl", "construct_platforms")

filegroup(
    name = "distribution",
    srcs = glob(["*.bzl"]) + [
        "BUILD.bazel",
    ],
    visibility = ["//python:__pkg__"],
)

construct_platforms(
    platforms = PLATFORMS.items(),
    python_versions = TOOL_VERSIONS.keys(),
)
