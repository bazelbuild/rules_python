import tempfile

from python.runfiles import runfiles

r = runfiles.Create()

def main():
    with tempfile.TemporaryDirectory
    # TODO(phil): Assemble the wheels into a directory and run pypiserver.
